% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roll.R
\name{roll}
\alias{roll}
\alias{print.roll}
\alias{plot.roll}
\title{Roll dice from notation once}
\usage{
roll(notation, compare = FALSE)

\method{print}{roll}(x, ...)

\method{plot}{roll}(x, ...)
}
\arguments{
\item{notation}{A length-1 character string. A single dice term is \code{NdX},
\code{NdX+M}, \code{NdX-M}, or the count-omitted \code{dX} variants (case-insensitive
\code{d}, whitespace-tolerant), optionally with a keep selector \code{h}/\code{l} and an
optional count after the die size (e.g. \verb{2d20h}, \verb{4d6h3}, \verb{3d6l2}), which
keeps the highest (\code{h}) or lowest (\code{l}) \code{K} dice (defaulting to \code{K = 1}).
In the same slot a drop selector \code{dl}/\code{dh}/\code{d} instead discards dice and
sums the rest: \code{NdXdlK} drops the lowest \code{K}, \code{NdXdhK} drops the highest
\code{K}, and the shorthand \code{NdXdK} drops the lowest \code{K}; a missing count drops
one die. Drop is the inverse spelling of keep, so \code{NdXdlK} is \code{NdXh(N-K)}
and \code{NdXdhK} is \code{NdXl(N-K)}; the conventional D&D roll \verb{4d6dl1} (drop the
lowest of four d6) is \verb{4d6h3}. The drop count \code{K} must satisfy
\verb{1 <= K <= N - 1} (drop at least one, leave at least one). A per-die marker
may follow the die size, before any keep or drop selector or modifier:
either an explode marker or a reroll marker, but not both (they are
mutually exclusive within a term). The explode marker is \code{!} (rerolls a
maximum-face die exactly once and sums the two faces; the extra die does
not itself explode) or \verb{!!} (keeps rerolling while the maximum recurs,
capped at 100 chained rerolls per die). So \verb{2d6!}, \verb{2d6!!}, \verb{4d6!h3}, and
\verb{2d6!+1} are all valid. When a \verb{!!} die reaches the cap, \code{roll()} emits a
warning while still returning a valid roll. The reroll marker is \code{rT}
(rerolls any die showing \verb{<= T} exactly once and keeps the new value
unconditionally, even if it is also \verb{<= T}) or \code{rrT} (rerolls a die
showing \verb{<= T} repeatedly until it lands strictly above \code{T}), where the
threshold \code{T} is required and bounded \verb{1 <= T <= X - 1}. Contrast the
explode marker: reroll replaces the die's value, it does not sum. So
\verb{2d6r1}, \verb{1d20rr1}, \verb{4d6r1h3}, and \verb{2d6r1+2} are all valid, and reroll
never warns. Several such terms, plus bare integer constants, may be
joined with \code{+} or \code{-} into one notation (e.g. \verb{1d20+1d6}, \verb{2d20h+2d20l},
\verb{1d20+1d6+1d4+3}); at least one dice term is required and each keep or drop
selector applies within its own term only.

A trailing success comparator turns the whole notation into a
success-counting pool: \code{NdX>=T}, \code{NdX>T}, \code{NdX<=T}, or \code{NdX<T} against an
integer target \code{T} (e.g. \verb{5d10>=8}, \verb{6d6>=5}). The outcome is then a count
of dice that satisfy the comparator, an integer in \verb{0..N}, not a summed
total. A die succeeds when its face satisfies the comparator against \code{T},
with per-die probability \code{p} equal to the fraction of \verb{1..X} faces that
satisfy it, so the count is exactly Binomial(\code{N}, \code{p}). A success-counting
notation is a single bare dice term with a comparator: it takes no keep or
drop selector, explode marker, reroll marker, or modifier, and does not
join with other terms or constants. See \code{\link[=roll_distribution]{roll_distribution()}} to summarise
many rolls.}

\item{compare}{A length-1 logical. When \code{TRUE}, printing the roll also
shows where the total sits within the notation's full theoretical outcome
distribution: a header line stating what percent of outcomes the roll
beats, then a text histogram of the exact outcome probabilities with the
rolled total's bar marked. Defaults to \code{FALSE}, which prints only the
roll itself. The comparison is computed, not sampled, so a given
(notation, total) pair always reports the same standing.}

\item{x}{A \code{roll} object, as returned by \code{roll()}.}

\item{...}{Ignored, for compatibility with the \code{\link[=print]{print()}} and \code{\link[=plot]{plot()}}
generics.}
}
\value{
A \code{roll} object: a list with \code{dice} (integer vector listing every
die rolled, the concatenation of all dice terms in term order), \code{total}
(integer scalar grand total: the sum of each term's signed contribution),
\code{kept} (the dice that contribute, the concatenation of each term's kept
dice in term order), \code{terms} (the per-term breakdown, each element a list
with the term's parsed fields plus its \code{dice}, \code{kept}, and \code{subtotal}),
the original \code{notation}, and \code{compare} (the logical flag controlling the
print method). For a single-term notation the parsed components \code{n}, \code{x},
\code{m}, \code{keep}, \code{keep_n}, \code{explode}, \code{reroll}, \code{reroll_t} are also present at
the top level; they are omitted for a multi-term notation, where per-term
access via \code{terms} is required. For an exploding or reroll term \code{dice}
still lists every physical die including rerolls in draw order (for a
reroll term, the rerolled-away faces are listed too), and \code{kept} lists the
kept per-die values. For a success-counting notation the outcome is a
success count, not a summed total: \code{total} (also \code{successes}) is the number
of dice that satisfy the comparator (\verb{0..N}), \code{successful} is the subset of
\code{dice} that satisfy it, and \code{success} is \code{TRUE}; a summed-total roll
carries none of these success fields.
}
\description{
Parses a dice-notation string and simulates a single roll. A notation may
be a sum of several terms joined by \code{+}/\code{-} (for example \verb{1d20+1d6+3}); each
dice term draws \code{N} independent uniform values from \verb{1..X}, keeps the
highest/lowest \code{K} when it carries a selector, and contributes its kept sum
plus its own modifier \code{M}. A \code{-} before a term subtracts that term's whole
contribution. Bare integer terms (constants) are added directly. The grand
\code{total} is the sum of every term's signed contribution.
}
\details{
\code{plot()} returns a themed \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} bar chart of the notation's
exact theoretical outcome distribution, with the rolled total's bar
highlighted and its percentile standing shown in the subtitle. The plot
always shows the theoretical distribution and never reads \code{compare}, which
remains a print-only switch. The returned object auto-prints when called at
the top level and can be captured and extended with \code{+}.
}
\examples{
set.seed(8)
roll("2d20+2")
roll("d6")
roll("4d6h3")
roll("1d20+1d6+3")
roll("5d10>=8")
roll("2d6", compare = TRUE)

}
