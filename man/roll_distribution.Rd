% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{roll_distribution}
\alias{roll_distribution}
\alias{print.roll_distribution}
\alias{plot.roll_distribution}
\title{Summarise the outcome distribution of repeated rolls}
\usage{
roll_distribution(notation, n)

\method{print}{roll_distribution}(x, ...)

\method{plot}{roll_distribution}(x, ...)
}
\arguments{
\item{notation}{A length-1 character string. A single dice term is \code{NdX},
\code{NdX+M}, \code{NdX-M}, or the count-omitted \code{dX} variants (case-insensitive
\code{d}, whitespace-tolerant), optionally with a keep selector \code{h}/\code{l} and an
optional count after the die size (e.g. \verb{2d20h}, \verb{4d6h3}), keeping the
highest/lowest \code{K} dice per roll (defaulting to \code{K = 1}). An explode marker
may follow the die size, before any keep selector or modifier: \code{!} rerolls
a maximum-face die once and sums both faces, \verb{!!} rerolls while the maximum
recurs, capped at 100 chained rerolls per die (e.g. \verb{2d6!}, \verb{2d6!!},
\verb{4d6!h3}). Sampling is bounded by the same cap so it always terminates;
\code{roll_distribution()} does not itself warn on the cap. Several such terms,
plus bare integer constants, may be joined with \code{+} or \code{-} into one
notation (e.g. \verb{1d20+1d6}, \verb{2d20h+2d20l}); at least one dice term is
required and each keep selector applies within its own term only. A
trailing success comparator (\code{NdX>=T}, \code{NdX>T}, \code{NdX<=T}, \code{NdX<T} against an
integer target \code{T}, e.g. \verb{5d10>=8}, \verb{6d6>=5}) turns the whole notation into
a success-counting pool: each simulated roll is then a count of dice that
satisfy the comparator (\verb{0..N}), not a summed total. A success-counting
notation is a single bare dice term with a comparator (no keep selector,
explode marker, modifier, join, or constant).}

\item{n}{Number of whole-notation rolls to simulate. A positive integer.}

\item{x}{A \code{roll_distribution} object, as returned by \code{roll_distribution()}.}

\item{...}{Ignored, for compatibility with the \code{\link[=print]{print()}} and \code{\link[=plot]{plot()}}
generics.}
}
\value{
A \code{roll_distribution} object: a list with \code{counts} (an integer
vector of observed totals, named by outcome value, ordered ascending;
zero-count outcomes are omitted), \code{range} (the theoretical \code{c(min, max)}
of a grand total, the sum across terms of each term's own min/max plus the
signed constants), \code{n}, \code{terms} (the parsed per-term breakdown), and the
original \code{notation}. For a single-term notation the parsed components
\code{dice_n}, \code{x}, \code{m}, \code{keep}, \code{keep_n} are also present at the top level;
they are omitted for a multi-term notation. For a success-counting notation
the outcome is a success count rather than a summed total: \code{range} is the
success-count range \code{c(0, N)}, \code{counts} are observed success counts, and
\code{success} is \code{TRUE}; a summed-total distribution carries no \code{success} field.
Its \code{print()} method renders the counts and a text histogram for the
console.
}
\description{
Simulates \code{n} whole-notation rolls and summarises the distribution of the
resulting totals. The distribution is sampled (not computed analytically),
so results vary run to run unless a seed is fixed with \code{\link[=set.seed]{set.seed()}}. A
notation may be a sum of several terms joined by \code{+}/\code{-}; each simulated
roll draws every dice term, applies each term's keep selector (\code{h}/\code{l})
within that term before summing, and adds the term modifiers and any bare
integer constants, so each total is the grand total across all terms.
}
\details{
\code{plot()} returns a themed \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} bar chart of the sampled
counts across the notation's theoretical total range. The returned object
auto-prints when called at the top level and can be captured and extended
with \code{+}.
}
\examples{
set.seed(1)
roll_distribution("2d6", n = 1000)
roll_distribution("4d6h3", n = 1000)
roll_distribution("1d20+1d6", n = 1000)

}
